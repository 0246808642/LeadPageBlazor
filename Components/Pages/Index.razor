@page "/"
@layout Components.Layout.MainLayout

<!-- Hero section -->
<Hero />

<!-- Galeria de bolos -->
<section class="relative py-20 bg-white" id="bolos">
    <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-pink-400 to-rose-400"></div>
    <Gallery />
</section>

<!-- Depoimentos -->
<section class="relative py-20 bg-pink-50" id="depoimentos">
    <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-rose-400 to-pink-400"></div>
    <Testimonials />
</section>

<!-- Formulário de contato -->
<section class="relative py-20 bg-white" id="contato">
    <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-pink-400 to-rose-400"></div>
    <ContactForm />

</section>